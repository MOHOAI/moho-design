# سجل التغييرات

## 0.2.1 — تحسين قابلية الاكتشاف

- إضافة كلمات مفتاحية مناسبة لـ npm وبيانات issues للمستودع.
- إضافة صفحة وثائق ثابتة بالعربية والإنجليزية مع canonical metadata وOpen Graph وJSON-LD صادق مطابق للمحتوى الظاهر.
- إضافة `sitemap.xml` و`robots.txt` لموقع الوثائق.
- إضافة workflow لنشر مجلد `docs/site` إلى GitHub Pages.
- إضافة ملاحظات المصادر الرسمية حول structured data وsitemaps وسلوك README في npm.


## 0.2.0 — 2026-08-14

أضيفت هوية تصميم MOHO الأصلية فوق مواصفة MO 1.0، مع سجل قدرات يتجاوز 100 ميزة مصنفة إلى `core` و`adapter` و`planned` حتى تكون حدود التنفيذ صريحة.

أضيف `composeMoDocuments` و`createMohoSystem` لدمج عدة مصادر MO في نظام واحد، مع كشف تعارضات السمات واختيار سياسة `error` أو `first-wins` أو `last-wins`.

أضيف كتالوج مجاني مضمّن metadata فقط يحتوي 389 إدخالًا مستخرجًا من بيانات npm المتاحة وقت البناء. لا تُشحن أي مكتبات طرف ثالث كاعتماديات تشغيلية، ويظل كل إدخال `catalog-only` إلى أن يمر بمراجعة مصدر ورخصة واختبارات adapter مستقلة.

أضيفت واجهات `MOHO_FEATURES` و`listMohoFeatures` و`MOHO_FREE_CATALOG` و`listMohoCatalog` و`getMohoCatalogEntry`، مع اختبارات للبحث والدمج وحجم الكتالوج.

أضيفت طبقة adapters اختيارية عبر `defineMohoAdapter` و`createMohoAdapterRegistry` و`catalogEntryToAdapterManifest`، من دون تثبيت أو تحميل أي اعتماديات خارجية تلقائيًا.

تحسنت تعريفات السمات المتداخلة ومترجم CSS، وأعيدت كتابة الوثائق لتوضيح حدود «المجاني» وعدم مساواة إدراج الرابط بإعادة توزيع الأصل.

## 0.1.0 — 2026-08-14

- إضافة مواصفة MO 1.0 بامتداد `.mo`.
- إضافة محلل آمن ومدقق بنية مع أخطاء منظمة.
- إضافة التحويل إلى CSS وJSON.
- إضافة أداة `moho` لسطر الأوامر.
- إضافة مثال عربي الاتجاه يضم سمات الألوان والمسافات والطباعة والمكوّنات والتكاملات المجانية.
- إضافة مخطط JSON، README، اختبارات، ورخصة MIT.
